# v1.3.0 家庭重大决策体验与计算可靠性

## v1.3.1 增量更新修复

新增 state_diff.py --mode update：未提及的资料保留，null 表示未知而不是删除，数组按整组替换。修复布尔值与数字被误判相同、空对象变化未被识别、住房贷款误归类房价变化及 Deadline 分类遗漏。总计108项测试通过。工作流结构仍为1.3.0，无需仅因本补丁重新初始化1.3.0运行。

v1.3.0 的 GitHub CI 已通过：commit 7fa5f19b6f3e1bb6b62d30e934c1896f501629a1。下列对话记录对应其发布前的指令版本；补丁的增量合并行为由新增单元测试验证，不宣称重新做了跨模型评测。

v1.3.1 发布后，已从远端重新克隆 commit 6c76ce9abf6d44ec7aca07fa0e00d07439b3d515，108项测试、38例结构校验、完整工作流演示与按月现金流示例均通过；对应 GitHub CI 成功。未安装进用户默认 Skill 目录，持久安装行为仍由具体客户端验证。

## 用户体验

- 首轮复用已知信息、指出待验证矛盾、最多询问三个独立项；统一主文件、字段池和示例。
- 收入主体/税前税后、资产可用性、风险担忧保持原义；年龄不单独触发职业风险。
- 目标、期限、不可接受代价进入决策；夫妻分歧及时展开，纳入保持现状与可逆方案。
- 默认一页摘要，完整计算与门禁仍保留；安装话术明确本轮读取与持久安装的区别。

## 计算与复算

- 缺失核心输入、错误上下文格式、未知债务、非有限数值、布尔金额不再默认为零。
- 月供计算支持零利率与极小利率，拒绝不足一个月或非整月期限。
- 新增按月现金流模拟，区分储备触线和实际现金缺口，可处理收入变化与一次性支出。
- 空融资结果不再通过完整性检查；旧校验绑定具体输出，复算自动失效下游。
- 新失败会撤销旧推荐门禁；最终输出前重新检查。压力测试必须有数字与现金流恒等式。
- 运行工作流版本升级为1.3.0；旧运行需从确认后的输入重新初始化。
- 修复原完整演示遗漏完整性门禁的问题，使用真实脚本计算虚构输入，未核实政策保持条件化。

## 本轮验证

- 101项 unittest 通过，包含实际计算→完整性检查→最终输出→更正重算的端到端测试。
- 原38个 benchmark 案例结构与覆盖检查通过；这不是38个 Agent 回答通过。
- 10个实际对话案例、12轮回答：由两个独立上下文评测代理作答，主代理逐项语义审阅，通过全部案例。原始回答、证据片段和 hash 绑定保存在 evals/results/。
- 独立的是上下文，不是模型供应商；同一会话模型继承，准确后端模型ID不可见。没有跨模型认证，也没有当前政策或真实家庭结果验证。
- Skill 结构校验通过。全部案例为虚构，不含真实家庭数据。

## 已知限制

关键词路由仍需 Agent 语义判断；脚本不解析任意自然语言。现金流按月净额模拟，忽略月内支付顺序，不包含投资回报。对话审阅工具核查证据完整性，不代替审阅者判断。持久安装与跨会话自动调用还取决于客户端能力。
