# v1.3.0 家庭重大决策体验与计算可靠性

## v1.3.3 跨模型自动触发修复

通过 Skill Installer 安装到本机默认 Codex Skills 目录后，用 gpt-5.5 在空目录启动全新临时会话，输入没有点名 Skill 的换房/职业问题。

1. 缺少 agents/openai.yaml 时，模型没有读取 Skill，直接给统一现金垫和月供比例，FAIL。
2. 增加自动调用元数据后，模型自动读取 SKILL.md，但仍跳过首次问诊并给阈值，FAIL。
3. 将首次回复门禁移到SKILL.md最前部、要求资料不足时完整读取intent-to-intake.md后，新会话自动读取两份文件，不再联网，不给买卖方向和统一比例，只复用年龄/孩子信息、说明换房与职业弹性的矛盾，并询问换房目的、家庭税后收入、含房贷的年支出，PASS。

这次验证证明 Codex CLI + gpt-5.5 的自动触发和首次行为通过，不代表其他客户端或所有模型已经通过。gpt-5.6-luna 因本机 Codex CLI 版本过旧无法启动，未进入模型回答阶段。CLI 同时报告模型缓存格式警告和部分无关连接器认证过期；这些没有阻止gpt-5.5完成本地Skill读取与回答。

v1.3.3 新增 `agents/openai.yaml`，明确中文显示名、默认提示和隐式调用；安装自检将该文件列为必需。总计122项unittest通过。实际失败与修复过程保留在evals/results，避免只保存成功样例。

## v1.3.2 完整规划与安装验收

额度中断前尚未覆盖完整规划实答、隔离安装和跨模型表现。本轮已完成前两项：

- 隔离目录通过官方 Skill Installer 从 GitHub 安装；安装副本108项测试和完整演示通过。新增 check_installation.py，可验证文件引用、工作流hash和本地计算，同时明确不代表客户端自动调用已验证。
- 独立上下文首次完整规划在 FINANCING_ANALYSIS 真实阻塞：普通30年全额摊还按揭没有续贷，却被强制要求续贷失败测试。
- 修复后，相同虚构案例重新初始化并完成全部适用步骤。普通按揭以无续贷、无大额到期本金和零尾款证明“不适用”，月供仍独立复算；合同未核实，因此最终保持 CONDITIONAL_PASS。
- 完整试用对600万、500万、400万元住房计算9条36个月现金路径。600万元方案成交即跌破用户给定储备线，500万和400万元方案在模拟期内未触线；这是虚构条件下的行为验证，不是现实购房建议。
- 控制器现在自行复算当前基线的净资产与年结余；外部校验报告即使声称PASS，也不能掩盖恒等式错误。校验领域与工作流步骤不匹配时拒绝记录。
- 总计120项 unittest 通过，包含普通按揭条件分支、月供独立复算、伪PASS拦截、安装自检及条件化端到端流程。

v1.3.2 代码提交 510cec58b3657a835bb0f08e1d88b38e416a8a7d 已发布。随后通过 Skill Installer 从该远端提交重新安装到全新隔离目录；安装副本120项测试、check_installation.py 和完整演示均通过，对应 GitHub CI 成功。

跨模型已有gpt-5.5的一条首次问诊用例通过；完整规划仍只在当前会话模型的独立上下文验证，尚未用gpt-5.5重跑。其他客户端的自动触发仍未验证。没有消耗用户的额度重置券。

## v1.3.1 增量更新修复

新增 state_diff.py --mode update：未提及的资料保留，null 表示未知而不是删除，数组按整组替换。修复布尔值与数字被误判相同、空对象变化未被识别、住房贷款误归类房价变化及 Deadline 分类遗漏。总计108项测试通过。工作流结构仍为1.3.0，无需仅因本补丁重新初始化1.3.0运行。

v1.3.0 的 GitHub CI 已通过：commit 7fa5f19b6f3e1bb6b62d30e934c1896f501629a1。下列对话记录对应其发布前的指令版本；补丁的增量合并行为由新增单元测试验证，不宣称重新做了跨模型评测。

v1.3.1 发布后，已从远端重新克隆 commit 6c76ce9abf6d44ec7aca07fa0e00d07439b3d515，108项测试、38例结构校验、完整工作流演示与按月现金流示例均通过；对应 GitHub CI 成功。未安装进用户默认 Skill 目录，持久安装行为仍由具体客户端验证。

## 用户体验

- 首轮复用已知信息、指出待验证矛盾、最多询问三个独立项；统一主文件、字段池和示例。
- 收入主体/税前税后、资产可用性、风险担忧保持原义；年龄不单独触发职业风险。
- 目标、期限、不可接受代价进入决策；夫妻分歧及时展开，纳入保持现状与可逆方案。
- 默认一页摘要，完整计算与门禁仍保留；安装话术明确本轮读取与持久安装的区别。

## 计算与复算

- 缺失核心输入、错误上下文格式、未知债务、非有限数值、布尔金额不再默认为零。
- 月供计算支持零利率与极小利率，拒绝不足一个月或非整月期限。
- 新增按月现金流模拟，区分储备触线和实际现金缺口，可处理收入变化与一次性支出。
- 空融资结果不再通过完整性检查；旧校验绑定具体输出，复算自动失效下游。
- 新失败会撤销旧推荐门禁；最终输出前重新检查。压力测试必须有数字与现金流恒等式。
- 运行工作流版本升级为1.3.0；旧运行需从确认后的输入重新初始化。
- 修复原完整演示遗漏完整性门禁的问题，使用真实脚本计算虚构输入，未核实政策保持条件化。

## 本轮验证

- 101项 unittest 通过，包含实际计算→完整性检查→最终输出→更正重算的端到端测试。
- 原38个 benchmark 案例结构与覆盖检查通过；这不是38个 Agent 回答通过。
- 10个实际对话案例、12轮回答：由两个独立上下文评测代理作答，主代理逐项语义审阅，通过全部案例。原始回答、证据片段和 hash 绑定保存在 evals/results/。
- 独立的是上下文，不是模型供应商；同一会话模型继承，准确后端模型ID不可见。没有跨模型认证，也没有当前政策或真实家庭结果验证。
- Skill 结构校验通过。全部案例为虚构，不含真实家庭数据。

## 已知限制

关键词路由仍需 Agent 语义判断；脚本不解析任意自然语言。现金流按月净额模拟，忽略月内支付顺序，不包含投资回报。对话审阅工具核查证据完整性，不代替审阅者判断。持久安装与跨会话自动调用还取决于客户端能力。
